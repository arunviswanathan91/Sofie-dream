// expo-background-fetch / expo-task-manager are not installed.
// Background backup is disabled; the app handles backup manually via the Backup screen.
export async function registerBackgroundBackupTask(): Promise<void> {
  // no-op
}
